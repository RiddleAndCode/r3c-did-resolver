module.exports = {
  capError: 'Invalid metadata field: controller and \
capabilityInvocation fields must not be set in verificationMethsods \
map.  They are taken from transaction ouputs.',
  invalidTxError: 'Invalid transaction: \
Transaction must have Metadata field'
};
